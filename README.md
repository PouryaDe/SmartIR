# SmartLink Proxy

To install, update, and manage sites, simply run the following command on your ubuntu server:

```bash
sudo bash -c "$(curl -sL https://raw.githubusercontent.com/PouryaDe/SmartIR/main/install.sh)"
```
